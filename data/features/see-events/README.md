# See Events Feature

Adds a `See Events` button to the top of the UI.

When clicked, it opens a full-window page listing:
- Upcoming meetings/events
- Recent meetings/events

The list is inferred from recent inbox emails using event-related keywords and lightweight date extraction.
