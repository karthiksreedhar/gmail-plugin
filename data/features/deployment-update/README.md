# Deployment Update Feature

Adds a top header button labeled `Deployment Update`.

When clicked, it opens a full-window page view that lists the most recent emails in the `Deployment Infrastructure` category.

Clicking an update card opens that email thread in the main app view.
